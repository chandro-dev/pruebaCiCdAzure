name: ejemplos
on:
  push:
    branches:
      - "master"
#    paths:
#      - "**.html"
jobs:
  este_es_un_job:
    runs-on: ubuntu-latest
    steps:
      - name: esto deberia hacer algo
        run: whoami
  este_es_otro_job:
    runs-on: ubuntu-latest
    steps:
      - name: Hace una accion
        run: ls -alF /
